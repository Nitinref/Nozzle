# Frontend

React + Tailwind dashboard for the Per-Customer AI Cost Radar demo.

## Environment

- `VITE_API_BASE_URL=http://localhost:8000` if you want to point directly at the backend

## Run

```powershell
cd frontend
npm install
npm run dev
```

## Build

```powershell
cd frontend
npm run build
```

